generator client {
  provider = "prisma-client-js"
}

datasource db {
  provider  = "postgresql"
  url       = env("DATABASE_URL") // 풀러(pgbouncer) 연결 — 런타임
  directUrl = env("DIRECT_URL") // 직접 연결 — 마이그레이션
}

model User {
  id         String   @id @default(cuid())
  studentNo  String   @unique
  name       String?
  department String?
  createdAt  DateTime @default(now())
  posts      Post[]
}

model Post {
  id          String   @id @default(cuid())
  type        String // RESUME | COVER_LETTER | PORTFOLIO
  description String? // 한 줄 메모 (선택)
  filePath    String?
  fileName    String?
  previewPath String? // HWP 등을 변환한 미리보기용 PDF 경로
  linkUrl     String?
  createdAt   DateTime @default(now())
  author      User     @relation(fields: [authorId], references: [id], onDelete: Cascade)
  authorId    String

  @@unique([authorId, type]) // 학생당 카테고리별 1개
}
